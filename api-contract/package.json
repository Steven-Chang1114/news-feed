{
  "name": "@news-feed/api-contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas defining every payload that crosses the network. The backend validates against them; the frontend infers its types from them.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
