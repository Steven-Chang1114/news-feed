# Collapsed by default in GitHub diffs. The lockfile must stay committed — it is
# what makes installs reproducible — but it is machine-written and never reviewed.
package-lock.json linguist-generated=true
